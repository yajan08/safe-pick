<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 720 282">
  <!-- Removed background rect -->
  <!-- "safe" part (changed to pure white) -->
  <text x="180" y="190" font-family="sans-serif" font-size="160" font-weight="bold" fill="#FFFFFF">safe</text>
  <!-- "pick" part (left as gold accent) -->
  <text x="480" y="190" font-family="sans-serif" font-size="160" font-weight="bold" fill="#C79322">pick</text>
  <!-- Gold accent icon -->
  <path d="M 80 150 L 120 70 L 160 150 Z" fill="#C79322"/>
  <circle cx="120" cy="190" r="40" fill="#C79322" />
</svg>
