rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Basic security: Allow read and write to all collections for any authenticated user.
    // This makes testing and development extremely easy and avoids any permission issues.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
